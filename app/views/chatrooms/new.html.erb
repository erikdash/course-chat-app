<%= form_for @chatroom do |room| %>
  <%= room.text_field :chat_room_name, placeholder: "Room Name", class: 'input-field', required: true %>

  <%= room.submit 'Submit', :class => 'submit-button' %>
<% end %>
